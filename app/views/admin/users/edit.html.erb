<h1>Редактиране на потребител</h1>
<br />
<%= error_messages_for :user %>
<%= form_for [:admin, @user] do |f| -%>

<%= render :partial => 'user_fields', :locals => { :f => f } %>
<br />
<p>
  <%= submit_tag 'Обновяване' %>
  <%= link_to "Отказ", admin_users_path %>
</p>
<% end -%>
