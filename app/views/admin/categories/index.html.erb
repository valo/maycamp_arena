<h1>Задачи по категории</h1>
<% for @cat in @categories do %>
  <h2><%= h(@cat.name) %></h2> 
  <%= link_to 'Промени', edit_admin_category_path(@cat) %> -
  <%= link_to 'Изтрий', admin_category_path(@cat), :method => :delete,  
         :confirm => 'Сигурни ли сте, че искате да изтриете категория ' + @cat.name + '?' %>
  <br><br>
  <% for @cat_prob in @cat.problems do %>
    <% if @cat_prob.contest.practicable and @cat_prob.contest.visible then %>
      <%= link_to @cat_prob.contest.name + " / " + @cat_prob.name, practice_path(:action => :open_contest, :contest_id => @cat_prob.contest_id) %><br>
    <% end %>
  <% end %>
<% end %>
<br><br>
<%= link_to "Нова категория", new_admin_category_path() %>
