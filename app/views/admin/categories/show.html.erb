<h1>Задачи по категории</h1>
<% for @cat in @categories do %>
  <h2><%= h(@cat.name) %></h2> 
  <%= link_to 'Edit category', edit_admin_categories_path(@cat) %> - 
  <%= link_to 'Delete category', admin_categories_path, method => :delete,
         :confirm => 'Сигурни ли сте, че искате да изтриете категория ' + @cat.name + '?', 
         :method => :post %>
  <br><br>
  <% for @cat_prob in @cat.problems do %>
    <% if @cat_prob.contest.practicable and @cat_prob.contest.visible then %>
      <%= link_to h(@cat_prob.name), practice_path(:action => :open_contest, :contest_id => @cat_prob.contest_id) %><br>
    <% end %>
  <% end %>
<% end %>

