<h2>Изпрати решение</h2>
<%= form_for @run, :url => { :action => :submit_solution, :contest_id => @contest.id }, :html => { :multipart => true } do |f| -%>
  <%= f.error_messages %>
  <p>
    <%= f.label :problem_id, "Задача:" %>
    <%= f.select :problem_id, @contest.problems.map { |p| [p.name, p.id] } %>
  </p>
  <p>
    <%= f.label :language, "Език:" %>
    <%= f.select :language, Run.languages %>
  </p>
  <p>
    <%= f.label :source_file, "Качи решение:" %>
    <%= f.file_field :source_file %>
    <%= f.submit "Изпрати", class: "btn btn-default" %>
  </p>
  <p>Или прати сорс кода направо от тук</p>
  <p>
    <%= f.label :source_code, "Сорс код:" %>
    <%= f.text_area :source_code, :class => "source_code" %>
  </p>
  <p>
    <%= f.submit "Изпрати", class: "btn btn-default" %>
  </p>
<% end -%>
