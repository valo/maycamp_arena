<h1>Problem <%= @problem.name %></h1>
<p>
  Name: <%= @problem.name %>
</p>
<p>
  Description: <%= @problem.about %>
</p>
<p>
  Time limit: <%= @problem.time_limit %>
</p>
<p>
  Memory limit: <%= @problem.text_memory_limit %>
</p>
<% if !@problem.all_files.blank? %>
  <h2>Files</h2>
  <p>
    <% for file in @problem.all_files %>
      <%= link_to File.basename(file), download_file_admin_contest_problem_path(@problem.contest, @problem, :file => File.basename(file)) %>
    <% end %>
  </p>
  <p><%= link_to "Purge files", purge_files_admin_contest_problem_path(@problem.contest, @problem) %></p>
<% end %>
<p>
  <%= link_to "Upload file", upload_file_admin_contest_problem_path(@problem.contest, @problem) %>
</p>
<p>
  <%= link_to "Back", admin_contest_problems_path(@problem.contest) %>
</p>